apiVersion: 1

datasources:
  - name: incident.io
    type: ricoberger-incidentio-datasource
    uid: incidentio
    access: proxy
    isDefault: false
    orgId: 1
    version: 1
    editable: true
    secureJsonData:
      apiKey: ${INCIDENTIO_API_KEY}
